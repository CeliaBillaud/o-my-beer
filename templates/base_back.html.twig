<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
    	<meta name="description" content="O’MyBeer, le site de référence dédié à l'univers brassicole, propose une large sélection de bières et marques. Il invite les amateurs à découvrir de nouveaux produits avec des infos détaillées.">

		<title>
            BackOffice - 
			{% block title %}
			{% endblock %}
		</title>
		{# <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">  #}
		{% block stylesheets %}{% endblock %}

		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
		<link rel="stylesheet" href="{{ asset( 'assets/css/styles-min.css' ) }}"> 
		<link rel="shortcut icon" href="{{ asset('favicon.png') }}" type="image/x-icon">
		{% block javascripts %}{% endblock %}
		</head>


		<body class="bg-success"> 
		
		<header class="sticky-top">
			<nav class="bg-dark navbar navbar-expand-lg p-2" role="navigation">
				<div class="container-fluid gap-3">

					<button class="navbar-toggler bg-primary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>

					<a class="d-inline-block align-top" href="{{path('app_back_homepage')}}">
						<a class="nav-link text-white d-sm-block d-md-block  d-lg-none mx-auto" title="Retourner a l'accueil du back-office" href="{{path('app_back_homepage')}}">Backoffice - O'My Beer</a>
						<a href="{{path('app_back_homepage')}}" title="Retourner a l'accueil du back-office"><img src="{{ asset('assets/img/omybeerwhite.webp') }}" alt="Logo OMy Beer" height="60"></a>
					</a>

					<div class="collapse navbar-collapse justify-content-lg-between flex-column flex-lg-row" id="navbarNavAltMarkup">

						<div class="navbar-nav flex-column flex-lg-row align-items-center ">
							<a class="nav-link {% if app.current_route == 'app_back_homepage' %}text-primary" aria-current="page" "{% else %}text-white{% endif %}" title="Accueil du back-office" href="{{path('app_back_homepage')}}">Accueil</a>
							<a class="nav-link {% if app.current_route == 'app_back_beer_list' %}text-primary" aria-current="page" "{% else %}text-white{% endif %}" title="Voir toutes les bières" href="{{path('app_back_beer_list')}}">Bières</a>
							<a class="nav-link {% if app.current_route == 'app_back_brand_list' %}text-primary" aria-current="page" "{% else %}text-white{% endif %}" title="Voir toutes les marques de bière" href="{{path('app_back_brand_list')}}">Marques</a>
							<a class="nav-link {% if app.current_route == 'app_back_type_list' %}text-primary" aria-current="page" "{% else %}text-white{% endif %}" title="Voir tous les types bière" href="{{path('app_back_type_list')}}">Types</a>
							<a class="nav-link {% if app.current_route == 'app_back_user_list' %}text-primary" aria-current="page" "{% else %}text-white{% endif %}" title="Voir tous les utilisateurs enregistrés" href="{{path('app_back_user_list')}}">Utilisateurs</a>
							<a class="nav-link {% if app.current_route == 'app_back_review_list' %}text-primary" aria-current="page" "{% else %}text-white{% endif %}" title="Voir tous les commentaires signalés" href="{{path('app_back_review_list')}}">Commentaires Signalés</a>
						</div>

						<div class="d-flex justify-content-between gap-lg-4">
							<div class="d-flex flex-column flex-lg-row align-items-center">
								<a class="btn btn-nav btn-primary text-white w-auto mb-2 mb-lg-0 ms-lg-2 " role="button" title="Retourner sur le front-office" href="{{path('app_homepage')}}">Front Office</a>
							</div>
							<!-- account icon appear on md sm xs screens -->
							<div class="d-flex align-items-center justify-content-center ">
								<a class="nav-link d-flex d-lg-block d-xl-block align-items-center" title="Se déconnecter" href="{{path('app_user_logout')}}">
									<i class=" d-flex justify-content-center m-1" style="font-size: 2em;">
										<svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="#F36F28" class="bi bi-person-circle" viewbox="0 0 16 16">
											<path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0"/>
											<path fill-rule="evenodd" d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8m8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1"/>
										</svg>
									</i>

									<p class="text-white mt-2 mb-0">Deconnexion</p>
								</a>
							</div>
						</div>

					</div>

				</div>
			</nav>
		</header>

		{# display flash messages #}
        {% for label, messages in app.flashes %}
		{% for message in messages %}
		<div class="alert alert-{{label}} mx-auto col-10 col-md-6 alert-dismissible fade show text-center" role="alert">
			{{ message }}
			<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
		</div>
		{% endfor %}
		{% endfor %}
		
		{% block body %}
		
		{% endblock %}

			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
		</body>
	</html>
